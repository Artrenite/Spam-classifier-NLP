Spam NLP Classifier
(spam classifier using NLP and machine learning techniques)

Running the project

1. Install dependencies: pip install -r requirements.txt

2. Train the model: python -m src.train --data_path spam_NLP.csv

3. Run the app: streamlit run app.py
